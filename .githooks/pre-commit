#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "$0")/.." && pwd)"
cd "$repo_root"

if [[ -x "scripts/check_repo_sync.sh" ]]; then
  bash scripts/check_repo_sync.sh
else
  echo "Missing scripts/check_repo_sync.sh"
  exit 1
fi
